const fs=require('fs'); const path=require('path'); function walk(dir) { const files=fs.readdirSync(dir); for (const file of files) { if (file==='node_modules' || file==='.next' || file==='.git') continue; const p=path.join(dir, file); try { const stat=fs.statSync(p); if (stat.isDirectory()) walk(p); else if (p.endsWith('.ts') || p.endsWith('.tsx') || p.endsWith('.js')) { const c=fs.readFileSync(p, 'utf-8'); if (c.includes('FALLBACK')) console.log(p); } } catch(e){} } } walk('.');
