import { InstallerLoginClient } from "@/components/installer/InstallerLoginClient";

export const metadata = {
  title: "Installer Login | TEAM CCTV",
  description: "Secure OTP access for field operations and installers.",
};

export default function InstallerLoginPage() {
  return <InstallerLoginClient />;
}
