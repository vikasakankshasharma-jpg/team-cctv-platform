import { Project, SyntaxKind } from "ts-morph"; import fs from "fs"; const project = new Project({ tsConfigFilePath: "tsconfig.json" }); const filesToProcess = [ "components/wizard/WizardClientV2.tsx", "components/quotation/ConfiguratorView.tsx", "components/quotation/DynamicVariantGenerator.tsx" ]; const newTranslations = {}; function toSnakeCase(str) { let s = str.trim().replace(/\s+/g, "_").replace(/[^a-zA-Z0-9_]/g, "").toLowerCase(); return "wz_" + s.substring(0, 30); } for (const filePath of filesToProcess) { console.log("Processing", filePath); const sourceFile = project.getSourceFileOrThrow(filePath); const jsxTexts = sourceFile.getDescendantsOfKind(SyntaxKind.JsxText); for (let i = jsxTexts.length - 1; i >= 0; i--) { const jsxText = jsxTexts[i]; const text = jsxText.getLiteralText(); const trimmed = text.trim(); if (trimmed.length > 2 && /[a-zA-Z]/.test(trimmed) && !trimmed.includes("{")) { const key = toSnakeCase(trimmed); newTranslations[key] = trimmed; const leadingSpace = text.match(/^\s*/)[0]; const trailingSpace = text.match(/\s*$/)[0]; jsxText.replaceWithText(`${leadingSpace}{t("${key}")}${trailingSpace}`); } } sourceFile.saveSync(); } fs.writeFileSync("new_translations.json", JSON.stringify(newTranslations, null, 2)); console.log("Done!");
